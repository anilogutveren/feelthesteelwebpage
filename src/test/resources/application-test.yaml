spring:
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    baseline-on-migrate: true
  security: false
  jwt:
    security: false
  datasource:
    driver-class-name: "org.testcontainers.jdbc.ContainerDatabaseDriver"
    url: "jdbc:tc:postgresql:11.7-alpine:///db"

